// Bookmark component should receive recipe being bookmarked. 
//The component checks the database to see if the recipe is already 
//bookmarked and sets state.


//domtokenlist toggle() method
//The toggle() method of the DOMTokenList interface removes an existing token from the list and returns false. 
//If the token doesn't exist it's added and the function returns true.

//When the user clicks the Bookmark button, the toggleBookmark method is called. 
//This method updates the state to reflect the bookmark status and 
//saves the information to the users profile on the database.

